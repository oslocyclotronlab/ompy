# API reference

The API reference is generated automatically from the Python source tree each
time the documentation is built. All modules under `src/ompy/` are included,
so the navigation contains every public and internal component.

```{toctree}
:maxdepth: 2

autoapi/ompy/index
```
